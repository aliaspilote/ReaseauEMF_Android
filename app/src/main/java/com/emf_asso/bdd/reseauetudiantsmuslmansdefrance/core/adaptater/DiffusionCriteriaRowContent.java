package com.emf_asso.bdd.reseauetudiantsmuslmansdefrance.core.adaptater;

/**
 * Created by Omar on 22/12/2015.
 */
public class DiffusionCriteriaRowContent { //extends ArrayAdapter<DiffusionCriteria> {
/*
    DiffusionCriteriaRowContent(Context ctx) {
        super(ctx, R.layout.row, R.id.label, items);
    }

    @Override
    public View getView(int position, View convertView,
                        ViewGroup parent) {
        View row = super.getView(position, convertView, parent);
        ImageView icon = (ImageView) row.findViewById(R.id.icon);
        if (items[position].length() > 4) {
            icon.setImageResource(R.drawable.delete);
        } else {
            icon.setImageResource(R.drawable.ok);
        }
        TextView size = (TextView) row.findViewById(R.id.size);
        size.setText(String.format(getString(R.string.size_template),
                items[position].length()));
        return (row);
    }
*/
}
